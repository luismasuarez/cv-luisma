export type TLayoutProps = {
  children: ReactNode | ReactNode[];
};

type TExperience = {
  position: string;
  company: string;
  time: string;
  duration: string;
  location: string;
  modality: string;
  url: string;
  aptitudes: string[];
};
